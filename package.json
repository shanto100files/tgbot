{
  "name": "vega-api-server",
  "version": "1.0.0",
  "description": "Backend API server for Vega providers - movie/series streaming link aggregator",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build:providers": "node build-providers.js",
    "setup": "git clone --depth 1 https://github.com/vega-org/vega-providers.git _providers && cd _providers && npm install && npm run build",
    "update": "cd _providers && git pull && npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "winston": "^3.17.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
